# vless2socks
Turn VLESS links into local SOCKS5 endpoints on Windows. One GUI manages unlimited proxies over a built-in Python engine or Xray-core, with live status, geo-IP country detection, auto-reconnect, encrypted AES-256-GCM backups, system tray and EN/RU interface. CLI mode and full tunnel diagnostics included.
